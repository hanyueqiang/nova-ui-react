# 简介

**Nova UI** 是一个现代的、高度可定制的 React AI UI 组件库。

它提供了开箱即用的组件，用于构建对话式 AI 界面，包括支持 Markdown 的消息气泡、自适应的输入区域和打字机效果。

## 为什么选择 Nova UI？

构建一个精致的 AI 聊天界面需要处理许多小细节：
- 安全且美观地渲染 Markdown。
- 代码块的语法高亮。
- 支持快捷键（Enter 发送，Shift+Enter 换行）的自适应文本框。
- 流式文本效果和光标动画。

Nova UI 将所有这些复杂性抽象成一组干净、易用的 React 组件。
